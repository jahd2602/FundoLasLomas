{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Planta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Columna</th>
                <th>Fila</th>
                <th>Estado</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('planta_show', { 'id': entity.id }) }}">{{ entity.columna }}</a></td>
                <td>{{ entity.fila }}</td>
                <td>{{ entity.estado }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('planta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('planta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('planta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
