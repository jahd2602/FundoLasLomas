{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EstadoAmbiente list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Fecha</th>
                <th>Temperatura</th>
                <th>Humedad</th>
                <th>Presionambiental</th>
                <th>Observaciones</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('estadoambiente_show', { 'id': entity.id }) }}">{{ entity.fecha }}</a></td>
                <td>{{ entity.temperatura }}</td>
                <td>{{ entity.humedad }}</td>
                <td>{{ entity.presionAmbiental }}</td>
                <td>{{ entity.observaciones }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('estadoambiente_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('estadoambiente_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('estadoambiente_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
