{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Fumigacion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Descripcion</th>
                <th>Observacion</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('fumigacion_show', { 'id': entity.id }) }}">{{ entity.descripcion }}</a></td>
                <td>{{ entity.observacion }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('fumigacion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('fumigacion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('fumigacion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
