{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Problema list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Descripcion</th>
                <th>Fecha</th>
                <th>Resuelto</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('problema_show', { 'id': entity.id }) }}">{{ entity.descripcion }}</a></td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.resuelto }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('problema_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('problema_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('problema_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
