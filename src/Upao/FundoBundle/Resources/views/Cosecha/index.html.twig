{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cosecha list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Totalkilos</th>
                <th>Kilosdisponibles</th>
                <th>Fecha</th>
                <th>Observaciones</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cosecha_show', { 'id': entity.id }) }}">{{ entity.totalKilos }}</a></td>
                <td>{{ entity.kilosDisponibles }}</td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.observaciones }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cosecha_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cosecha_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cosecha_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
